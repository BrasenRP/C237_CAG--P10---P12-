<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <title>Proddy Calendar App</title>
        <style>
            #btns {
            background-color: #FA7F6E;
            color: white
          }
        </style>
    </head>
    <body>
      <%- include("navBar/navBar.ejs")%>
      
      <div class="container">
        <br>
        <div class="text-center"><h2>Event</h2></div>
        <br>
        <% if (event) { %>
            <div>
                <p><b>Event Name:</b><br> <%= event.event_name %></p>
                <p><b>Date:</b><br> <%= event.event_date %></p>
                <p><b>Time:</b><br> <%= event.event_time %></p> 
            </div>
        <% } else { %>
            <p>No event found.</p>
        <% } %>
        <a type="button" id="btns" class="btn" href ="/">Back</a>
    </div>
    </body>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</html>