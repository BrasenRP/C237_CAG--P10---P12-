<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <title>Proddy Calendar App</title>
        <style>
          #btns {
          background-color: #FA7F6E;
          color: white
        }
      </style>
    </head>
    <body>
      <%- include("navBar/navBar.ejs")%>
      
      <div class="container">
        <br>
        <div class="text-center"><h2>Update Product</h2></div>
        <br>
        <div class="text-center">
          <% if (event) { %> 
            <form action="/editEvent/<%= event.eventId %>" method="POST">
                <label for="name">Event Name:</label><br>
                <input type="text" id="name" name="event_name" value = "<%= event.event_name %>" required><br><br>

                <label for="date">Event Date:</label><br>
                <input type="date" id="date" name="event_date" value = "<%= event.event_date.toLocaleString("default", {year: "numeric"})%>-<%= event.event_date.toLocaleString("default", {month: "2-digit"})%>-<%= event.event_date.toLocaleString("default", {day: "2-digit"}) %> required>"<br><br>

                <label for="time">Event Time:</label><br>
                <input type="time" id="time" name="event_time" value ="<%= event.event_time %>" required><br><br>


                <button id="btns" class="btn" type="submit">Update Event</button>
            </form>
          <% } else { %>
              <p>No Event found.</p>
          <% } %>
          <a type="button" id="btns" class="btn" href ="/">Back</a>
        </div>
    </div>
    </body>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</html>