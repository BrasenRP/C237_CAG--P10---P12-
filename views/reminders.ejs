<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <title>Proddy Calendar App</title>
        <style>
          #btns {
          background-color: #FA7F6E;
          color: white
        }
        </style>
    </head>
    <body>
      <body>
        <%- include("navBar/navBar.ejs")%>
  
        <div class="container">
          <br>
          <div class="text-center"><h2>Your Reminders</h2></div>
          <br>
          <table class="table table-hover small text-center">
            <tbody>
              <% for(let i=0; i < reminders.length; i++) { %>
                <hr>
                <tr>
                  <td style="font-size: 15px; padding: 50px"><a href="/reminder/<%= reminders[i].reminderId %>" style="color:#FA7F6E"><%= reminders[i].reminder_date.getDate() %>/<%= reminders[i].reminder_date.getMonth() + 1%>/<%= reminders[i].reminder_date.getFullYear() %></a></td>
                  <td style="font-size: 15px; padding: 50px"><a href="/reminder/<%= reminders[i].reminderId %>" style="color:#FA7F6E"><%= reminders[i].reminder_time %></a></td>
                  <td style="font-size: 15px; padding: 50px"><a href="/reminder/<%= reminders[i].reminderId %>" style="color:#FA7F6E"><%= reminders[i].msg %></a></td>
                  <td style="font-size: 15px; padding: 50px"><a type="button" id="btns" class="btn" href="/editReminder/<%= reminders[i].reminderId%>">Edit</a></td>
                  <td style="font-size: 15px; padding: 50px"><a type="button" id="btns" class="btn" href="/deleteReminder/<%= reminders[i].reminderId%>" onclick="return confirm('Are you sure you want to delete this reminder?')">Delete</a></td>
                </tr>
              <% } %>
            </tbody>
          </table>
          <div class="d-flex" style="flex-wrap: wrap-reverse; flex-direction: row-reverse; margin-right: 5px;">
            <a type="button" id="btns" class="btn" href="/addReminder">Add a Reminder</a>
          </div>
        </div>
      </body>
    </body>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</html>