<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <title>Proddy Calendar App</title>
        <style>
          #btns {
          background-color: #FA7F6E;
          color: white
        }
      </style>
    </head>
    <body>
      <%- include("navBar/navBar.ejs")%>

      <div class="container">
        <br>
        <div class="text-center"><h2>Update Product</h2></div>
        <br>
        <div class="text-center">
          <% if (reminder) { %> 
            <form action="/editReminder/<%= reminder.reminderId %>" method="POST">

                <label for="date">Reminder Date:</label><br>
                <input type="date" id="date" name="reminder_date" value =<%=reminder.reminder_date.toLocaleString("default", {year: "numeric"})%>-<%= reminder.reminder_date.toLocaleString("default", {month: "2-digit"})%>-<%= reminder.reminder_date.toLocaleString("default", {day: "2-digit"})%> required><br><br>

                <label for="time">Reminder Time:</label><br>
                <input type="time" id="time" name="reminder_time" value =<%= reminder.reminder_time %> required><br><br>

                <label for="msg">Message:</label><br>
                <textarea cols="40" rows="5" type="text" id="msg" name="msg"><%= reminder.msg %></textarea><br><br>
            </form>

            <button id="btns" class="btn" type="submit">Update Reminder</button>

          <% } else { %>
              <p>No reminder found.</p>
          <% } %>
          <a type="button" id="btns" class="btn" href ="/">Back</a>
        </div>
    </div>

    </body>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</html>